# SUPER_ADMIN Password Reset Tool

Този директорий съдържа инструменти за обновяване на SUPER_ADMIN паролата директно в PostgreSQL базата данни.

## 📁 Файлове

- `reset-superadmin-password.sql` - PostgreSQL скрипт за обновяване на паролата
- `reset-superadmin-sql.sh` - Shell скрипт за автоматизация 
- `README.md` - Този файл с инструкции

## 🔧 Инсталация в контейнер

### Стъпка 1: Копиране на файловете
```bash
# Копирайте цялата папка в контейнера под root
sudo cp -r reset/ /root/superadmin-reset/
cd /root/superadmin-reset/
```

### Стъпка 2: Настройка на права
```bash
chmod +x reset-superadmin-sql.sh
chown root:root *
```

## 🚀 Използване

### Метод 1 (препоръчителен) - Автоматичен скрипт:
```bash
sudo /root/superadmin-reset/reset-superadmin-sql.sh
```

### Метод 2 - Директно изпълнение на SQL:
```bash
sudo psql -d invoiceapp -f /root/superadmin-reset/reset-superadmin-password.sql
```

## 🔐 Данни за влизане след обновяване

- **Username:** `superadmin`
- **Email:** `admin@invoiceapp.com`
- **Password:** `CHANGE_THIS_PASSWORD`
- **URL:** `http://localhost:3000`

## ⚙️ Конфигурация

Скриптът използва следните environment variables (по избор):

```bash
export DB_HOST=localhost      # PostgreSQL host
export DB_PORT=5432          # PostgreSQL port  
export DB_NAME=invoiceapp    # Database name
export DB_USER=postgres      # PostgreSQL user
```

## ⚠️ Изисквания

- PostgreSQL с `pgcrypto` extension
- Root/admin права за достъп до базата данни
- Достъп до PostgreSQL инстанцията

## 🔒 Сигурност

- Скриптът трябва да се изпълнява само с root права
- Паролата се кодира с BCrypt за съвместимост
- Препоръчва се изтриване на скрипта след употреба в production среда

## 📝 Забележки

- Скриптът обновява директно PostgreSQL базата данни без Spring Boot
- Автоматично включва `pgcrypto` extension ако не е налична
- Показва резултата от операцията за потвърждение