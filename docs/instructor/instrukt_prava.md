# Инструкции към AI: Права на потребителите за SaaS система за онлайн фактуриране и други документи

Целта е да дефинираме и имплементираме ролеви достъп в система с **много фирми**, като собственикът на SaaS продукта е **superadmin**, а всяка фирма има свой **administrator**, който плаща и управлява достъпа на потребители.

## 🧠 Какво да поискам от AI (примерна заявка)

> Напиши Go код (или обясни архитектурно), който да имплементира следната роля и контрол на достъп в моята система за онлайн фактуриране:

---

### 👑 Роли в системата:

#### 1. **SuperAdmin**
- Собственикът на целия SaaS продукт (аз).
- Има **глобален админ панел**.
- Може да:
  - вижда всички регистрирани фирми,
  - задава колко потребителя е платена квота за всяка фирма,
  - блокира фирми,
  - ресетва пароли, трие акаунти, т.н.

#### 2. **Administrator (на фирма)**
- Потребител, който е платил услугата за своята фирма.
- Има достъп до административен панел само за своята фирма.
- Може да:
  - кани потребители до лимита, определен от SuperAdmin (примерно 2 потребителя в безплатния план, 5 в платения и т.н.),
  - задава роля на потребител (фактурист, счетоводител),
  - редактира данни на фирмата (име, адрес, ДДС и т.н.),
  - преглежда всички фактури на фирмата.

#### 3. **Потребители на фирмата**:
- **Фактурист* user * – може да създава, редактира и изпраща фактури.
- **Счетоводител* accountant * – има само Read-only достъп до фактури (или експортиране).
- Всеки потребител е обвързан с точно една фирма.

---

### 🛠️ Какво очаквам като резултат от AI:

1. Примерна **Go структура** (model / struct) за потребител, роля и фирма.
2. Пример как се проверяват права в middleware (на база JWT или сесия).
3. Как да изградя **SuperAdmin панел**, който показва:
   - списък с фирми,
   - колко активни потребители има всяка,
   - какъв е техният лимит и дали са в нарушение.
4. Как **Administrator на фирма** може да:
   - кани потребители чрез e-mail покана или линк,
   - вижда колко потребители му остават.

---

### 💡 Бележки:

- Данните се съхраняват в PostgreSQL.
- Използвам GORM за ORM и Gin за HTTP framework.
- Възможно е всяка фирма да има собствен `organization_id`.
- Всеки потребител има роля и се свързва с фирма чрез foreign key.

---

### 🧩 Възможна структура на таблици (също може да поискам от AI):

- `users`:
  - id, email, name, password_hash, role (enum: superadmin, admin, fakturist, schetovoditel), organization_id
- `organizations`:
  - id, name, paid_user_limit (int), active (bool)
- `invitations`:
  - id, email, organization_id, role, token, status

---

## Примерна команда към AI:

> Напиши Go моделите и middleware за ролеви достъп в SaaS система, където има SuperAdmin, фирмени администратори и потребители (фактурист/счетоводител), с възможност SuperAdmin да задава лимити на потребители за всяка фирма.


